{% extends 'base.html.twig' %}

{% block title %}Administravimas{% endblock %}

{% block body %}
{{ form(inputForm) }}
{% if state == "submitted"  %}
    <pre>{{ dump(formData) }}</pre>
{% endif %}
{% endblock %}
